/**
 * hogaresModel.js — punto de entrada (compatibilidad).
 *
 * La lógica vive en models/hogares/ (varios archivos más cortos).
 * Este archivo solo reexporta para que el controller no cambie.
 */
module.exports = require("./hogares");
